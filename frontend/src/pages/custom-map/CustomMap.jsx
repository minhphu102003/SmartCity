import { Map } from "../../components/map";

const CustomMap = () => {
    return (
        <div className="w-full h-full">
            <Map isAuth={true} />
        </div>
    );
};

export default CustomMap;

import React from "react";
import { Map } from "../../components/map";

const Home = () => {
    return (
        <div className="w-full h-full">
            <Map />
        </div>
    );
};

export default Home;

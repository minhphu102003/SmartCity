import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const MapIcon = ({ icon, className }) => {
  return <FontAwesomeIcon icon={icon} className={className} />;
};

export default MapIcon;
